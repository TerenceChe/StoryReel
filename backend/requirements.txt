fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
sse-starlette>=2.0.0
httpx>=0.27.0
hypothesis>=6.98.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
edge-tts>=6.1.0
mutagen>=1.47.0
openai-whisper>=20231117
moviepy>=2.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
PyJWT[crypto]>=2.8.0

# Optional image-generation provider SDK. Only required when
# IMAGE_GEN_PROVIDER=openai is configured by the operator; the dependency
# container falls back to the disabled backend when this is not installed
# (see backend/dependencies.py). Kept optional so deployments that leave
# image generation disabled do not pull the provider SDK.
openai>=1.0.0
